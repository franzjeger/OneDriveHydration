--- crates/onedrive-daemon/src/dbus.rs
+++ crates/onedrive-daemon/src/dbus.rs
@@ -829,6 +829,24 @@
         Ok(url)
     }
 
+    async fn open_setup_wizard(
+        &self,
+        #[zbus(header)] header: Header<'_>,
+        #[zbus(connection)] connection: &zbus::Connection,
+    ) -> Result<(), Error> {
+        self.caller_permitted(&header, connection).await?;
+        std::thread::spawn(|| {
+            let _ = std::process::Command::new("onedrive-hydrationctl")
+                .arg("setup")
+                .stdin(std::process::Stdio::null())
+                .stdout(std::process::Stdio::null())
+                .stderr(std::process::Stdio::null())
+                .spawn();
+        });
+        Ok(())
+    }
+
     #[zbus(property)]
     fn enrollment_state(&self) -> String {
         let busy = self.enrollment_busy.load(Ordering::SeqCst);
